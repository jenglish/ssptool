//- report pages
extends layout
block content
  h1= report.title
  table.table.table-bordered
    thead
      tr
        each colname in report.columns
          th= colname
    tbody
      each rec in records
        tr
          each col in report.columns
            td: +showThe(rec, col)
